const firstArray = [5, 4, 3, 2, 1];
function bubbleSort(arr: number[]): number[] {
    const n = arr.length;
    let swapped: boolean;
{
        for () {
            for () {
                if () {

                }
            }
        }
    if (!swapped) {
        break;
    }
}
  return arr;
}
console.log(bubbleSort(firstArray));
